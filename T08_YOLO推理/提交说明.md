# T08 YOLO 推理 · 提交说明

> **怎么用（三步）**：① 把整个 `T08_… /` 文件夹复制一份；② 重命名成 `T08_<学号>_<姓名>/`；
> ③ 填完本文件、把代码放进 `代码/`、截图放进 `截图/`，打包交到群里的「作业收集表」。
>
> **最低只交两样：代码 + 关键截图**；本文件其余部分选填（写了更好，不写也没关系）。
> 命名：文档 `T08_2025xxxx_张三.md` · 截图 `T08_xxx.png` · 数据集/大文件另给网盘链接。
> 更多细则见 `00_起步/00-2_提交规范.md`。

---

## 0. 基本信息（必填）

| 项 | 内容 |
|---|---|
| 任务 | **T08 YOLO 推理** |
| 对应目录 | `07_YOLO推理/` |
| 学号 / 姓名 |  |
| 代码位置 | `代码/` 下文件名：______　或　仓库：______ · commit `______` |
| 提交日期 |  |
| 本次耗时 | ______ 小时（选填） |

> 权重是必备素材，离线环境不会自动下载，必须自己放进 `assets/`。

---

## 1. 本题要交什么

- [ ] 完成后的 `07_YOLO推理/hw_yolo.py`
- [ ] 画了检测框与类别名的结果图
- [ ] 终端里按置信度降序打印的检测列表截图

---

## 2. 关键截图（必交）

> 从你敲的命令开始，到结果出来**整段截**；只截最后一行无法排错。
> 建议命名：`T08_结果图.png`、`T08_终端.png`

（在此贴图，或写「见 `截图/xxx.png`」）

---

## 3. 完成标准自查（交前逐条打勾）

- [ ] `load_model`：加载 `yolov8n.pt`（路径找不到时会在候选目录里搜）
- [ ] `detect_objects`：解析 `xyxy / conf / cls` 并映射 `names`
- [ ] `count_by_class`：按类别计数（字典）
- [ ] `max_conf_target`：取置信度最高的目标
- [ ] `draw_dets`：把框和类别名画到图上
- [ ] 用 `assets/bus.jpg` 跑能检出 `person` 与 `bus`
- [ ] `yolov8n.pt` 确实在 `assets/` 下（没有它 T08 做不了，不是可选项）

---

## 4. 自测输出（选填）

```
（把自检命令的完整输出粘在这里，方便助教对照）
```

---

## 5. 卡点与解决（选填）

- 卡点：
- 怎么解决的：

---

## 6. AI 对话记录（选填）

> 卡住时问 AI 的那段对话，截图或复制文字都行；没用到 AI 就跳过。

---

## 7. 我学到了什么（选填）

---

## 8. 给下一届的建议（选填）

> 哪个坑最坑、哪段讲义不清楚、希望补什么。
