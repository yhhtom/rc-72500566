# T06 传统视觉三板斧（图测） · 提交说明

> **怎么用（三步）**：① 把整个 `T06_… /` 文件夹复制一份；② 重命名成 `T06_<学号>_<姓名>/`；
> ③ 填完本文件、把代码放进 `代码/`、截图放进 `截图/`，打包交到群里的「作业收集表」。
>
> **最低只交两样：代码 + 关键截图**；本文件其余部分选填（写了更好，不写也没关系）。
> 命名：文档 `T06_2025xxxx_张三.md` · 截图 `T06_xxx.png` · 数据集/大文件另给网盘链接。
> 更多细则见 `00_起步/00-2_提交规范.md`。

---

## 0. 基本信息（必填）

| 项 | 内容 |
|---|---|
| 任务 | **T06 传统视觉三板斧（图测）** |
| 对应目录 | `06_传统视觉/` |
| 学号 / 姓名 |  |
| 代码位置 | `代码/` 下文件名：______　或　仓库：______ · commit `______` |
| 提交日期 |  |
| 本次耗时 | ______ 小时（选填） |

> 这是最难的模块，多留点时间。做不出来先用 `06_传统视觉/examples/02b_hsv_trackbar.py` 把阈值调出来再写代码，能省一半时间。

---

## 1. 本题要交什么

- [ ] 完成后的 `06_传统视觉/hw_vision.py`（四个函数）
- [ ] 四张图的识别结果图（把质心 / 框画在图上）
- [ ] 说明你最终的 HSV 阈值，以及为什么这么取

---

## 2. 关键截图（必交）

> 从你敲的命令开始，到结果出来**整段截**；只截最后一行无法排错。
> 建议命名：`T06_结果图_quiz01.png` … `T06_结果图_quiz04.png`、`T06_终端.png`

（在此贴图，或写「见 `截图/xxx.png`」）

---

## 3. 完成标准自查（交前逐条打勾）

- [ ] `detect_red_circle` 命中真值 `(320, 240)`，容差 8 px
- [ ] `detect_blue_rect` 命中真值 `(320, 240)`，容差 8 px
- [ ] `detect_green_triangle` 命中真值 `(320, 293)`（图里有红色圆干扰）
- [ ] `detect_red_targets` 命中 `[(120,130), (453,373)]`（带噪点）
- [ ] 结果图写到 `out/*_result.png` 并一起提交
- [ ] 阈值是自己用 `examples/02b_hsv_trackbar.py` 调出来的
- [ ] 能说清「为什么先开运算后闭运算」「橙色为什么会混进红色掩膜」

---

## 4. 自测输出（选填）

```
（把自检命令的完整输出粘在这里，方便助教对照）
```

---

## 5. 卡点与解决（选填）

- 卡点：
- 怎么解决的：

---

## 6. AI 对话记录（选填）

> 卡住时问 AI 的那段对话，截图或复制文字都行；没用到 AI 就跳过。

---

## 7. 我学到了什么（选填）

---

## 8. 给下一届的建议（选填）

> 哪个坑最坑、哪段讲义不清楚、希望补什么。
