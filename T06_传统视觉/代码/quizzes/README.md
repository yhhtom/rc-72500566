# 正课课后题（题面·归档）

这 5 个文件是**正课阶段的原始题面**，为「一个字都不丢」而保留在此，路径与 `images/`、`examples/` 同级，
所以直接 `python quizzes/quiz_01.py` 仍能跑。

本教案的正式作业请写在这里：

| 原题 | 本教案对应 |
|---|---|
| `quiz_01.py` ~ `quiz_04.py` | `06_传统视觉/hw_vision.py` 的四个函数（T06） |
| `quiz_05.py`（全部自己写） | `06_传统视觉/hw_track_live.py`（T07 进阶） |

> 说明：`quizzes/` 与 `answers/` 属于**归档与助教资料**，生成学生包时可整体删除。
