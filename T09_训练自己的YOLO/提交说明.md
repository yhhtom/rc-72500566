# T09 训练自己的 YOLO · 提交说明

> **怎么用（三步）**：① 把整个 `T09_… /` 文件夹复制一份；② 重命名成 `T09_<学号>_<姓名>/`；
> ③ 填完本文件、把代码放进 `代码/`、截图放进 `截图/`，打包交到群里的「作业收集表」。
>
> **最低只交两样：代码 + 关键截图**；本文件其余部分选填（写了更好，不写也没关系）。
> 命名：文档 `T09_2025xxxx_张三.md` · 截图 `T09_xxx.png` · 数据集/大文件另给网盘链接。
> 更多细则见 `00_起步/00-2_提交规范.md`。

---

## 0. 基本信息（必填）

| 项 | 内容 |
|---|---|
| 任务 | **T09 训练自己的 YOLO** |
| 对应目录 | `08_训练自己的YOLO/` |
| 学号 / 姓名 |  |
| 代码位置 | `代码/` 下文件名：______　或　仓库：______ · commit `______` |
| 提交日期 |  |
| 本次耗时 | ______ 小时（选填） |

> 建议就训小球（乒乓球最好拍）——这一节的 `best.pt` 会直接用在 E4 滚球测速里，一次投入两处复用。CPU 慢就先把 epochs 降到 30、imgsz 降到 320 跑通流程。

---

## 1. 本题要交什么

- [ ] `dataset/` 整个目录（含 `data.yaml`，大文件给网盘链接）
- [ ] 训练得到的 `best.pt`（大文件给网盘链接）
- [ ] `results.png` 与终端最后一屏（mAP 指标）
- [ ] 一句话说明你的 mAP50 是多少、你觉得够不够用

---

## 2. 关键截图（必交）

> 从你敲的命令开始，到结果出来**整段截**；只截最后一行无法排错。
> 建议命名：`T09_results.png`、`T09_终端.png`、`T09_数据集目录.png`

（在此贴图，或写「见 `截图/xxx.png`」）

---

## 3. 完成标准自查（交前逐条打勾）

- [ ] 四级目录齐全：`images/train`、`images/val`、`labels/train`、`labels/val`
- [ ] 图片数 == 标签数（不配对的空标签会让模型学废）
- [ ] 训练集 ≥ 30 张（少了 mAP 一定难看）
- [ ] `data.yaml` 里 `path` / `train` / `val` / `names` 都对（`nc` 可省：`names` 写成 `0: 类名` 字典时自动推导），类别 id 从 0 开始
- [ ] 用随附示例数据集跑通时：`path` 写的是绝对路径，或训练时就在 `data.yaml` 所在目录里跑
- [ ] 标签每行 5 列且**已归一化到 0~1**（绝对像素是最常见的错误）
- [ ] `best.pt` 存在
- [ ] （可选）mAP50 ≥ 0.5；`results.png` 曲线在收敛

---

## 4. 自测输出（选填）

```
（把自检命令的完整输出粘在这里，方便助教对照）
```

---

## 5. 卡点与解决（选填）

- 卡点：
- 怎么解决的：

---

## 6. AI 对话记录（选填）

> 卡住时问 AI 的那段对话，截图或复制文字都行；没用到 AI 就跳过。

---

## 7. 我学到了什么（选填）

---

## 8. 给下一届的建议（选填）

> 哪个坑最坑、哪段讲义不清楚、希望补什么。
