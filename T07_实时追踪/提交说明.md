# T07 实时追踪（基础 + 进阶） · 提交说明

> **怎么用（三步）**：① 把整个 `T07_… /` 文件夹复制一份；② 重命名成 `T07_<学号>_<姓名>/`；
> ③ 填完本文件、把代码放进 `代码/`、截图放进 `截图/`，打包交到群里的「作业收集表」。
>
> **最低只交两样：代码 + 关键截图**；本文件其余部分选填（写了更好，不写也没关系）。
> 命名：文档 `T07_2025xxxx_张三.md` · 截图 `T07_xxx.png` · 数据集/大文件另给网盘链接。
> 更多细则见 `00_起步/00-2_提交规范.md`。

---

## 0. 基本信息（必填）

| 项 | 内容 |
|---|---|
| 任务 | **T07 实时追踪（基础 + 进阶）** |
| 对应目录 | `06_传统视觉/` |
| 学号 / 姓名 |  |
| 代码位置 | `代码/` 下文件名：______　或　仓库：______ · commit `______` |
| 提交日期 |  |
| 本次耗时 | ______ 小时（选填） |

> 进阶题对应原课后题 quiz_05，是「全自己写」的实战题——讲义只给需求与思路，参考实现不发。

---

## 1. 本题要交什么

- [ ] 实时追踪的截图或 5 秒录屏（素材视频或摄像头都行）
- [ ] 基础：跑满 30 帧的统计（帧数 / 命中数 / 帧率）
- [ ] 进阶：自己写的 `06_传统视觉/hw_track_live.py` + 它的自检输出

---

## 2. 关键截图（必交）

> 从你敲的命令开始，到结果出来**整段截**；只截最后一行无法排错。
> 建议命名：`T07_实时追踪.png`、`T07_进阶自检.png`、`T07_追踪.mp4`

（在此贴图，或写「见 `截图/xxx.png`」）

---

## 3. 完成标准自查（交前逐条打勾）

- [ ] 基础：≥ 30 帧、命中率 ≥ 70%、帧率 ≥ 10 FPS
- [ ] 没有摄像头时用素材视频（完全等价）
- [ ] 进阶：`1/2/3/4` 能切换红 / 蓝 / 绿 / 黄，画面显示当前颜色与 FPS
- [ ] 进阶：四种颜色都能定位，单帧 ≥ 15 FPS
- [ ] 进阶：代码里确实有「降低延迟（`BUFFERSIZE=1`）」「断流自动重连」「按键映射」「FPS 显示」
- [ ] 进阶：退出时 `release()` + `destroyAllWindows()`
- [ ] （虚拟机没有摄像头）用 `tools/stream_video.py` 推流或手机 DroidCam 拉流

---

## 4. 自测输出（选填）

```
（把自检命令的完整输出粘在这里，方便助教对照）
```

---

## 5. 卡点与解决（选填）

- 卡点：
- 怎么解决的：

---

## 6. AI 对话记录（选填）

> 卡住时问 AI 的那段对话，截图或复制文字都行；没用到 AI 就跳过。

---

## 7. 我学到了什么（选填）

---

## 8. 给下一届的建议（选填）

> 哪个坑最坑、哪段讲义不清楚、希望补什么。
